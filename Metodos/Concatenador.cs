class Concatenador
{
    public void Concatenar(string msg1, string msg2)
    {
        Console.WriteLine(msg1 + ' ' + msg2);
    }
    public void Concatenar(string msg1, string msg2, string msg3)
    {
        Console.WriteLine(msg1 + " " + msg2 + " " + msg3 + ".");
    }
}